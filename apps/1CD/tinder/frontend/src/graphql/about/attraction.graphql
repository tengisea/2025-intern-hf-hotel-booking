mutation updateAttraction($attraction: String!) {
  updateAttraction(attraction: $attraction) {
    email
  }
}

mutation UpdateGender($gender: String!) {
  updateGender(gender: $gender) {
    email
  }
}
